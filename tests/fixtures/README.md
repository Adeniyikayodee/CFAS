# Test fixture

`callin_sample.wav` is the CMU Sphinx sample utterance "go forward ten meters",
16 kHz mono PCM, from the pocketsphinx project (BSD-licensed). It stands in for a
listener call-in so the speech-to-text path runs on real speech, fully offline.
